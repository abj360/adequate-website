import React from 'react';
import '../css/cta.css';

const CTASection = () => {
    return (
        <section className="cta">
            <h2 className="cta-text">Ready to Take the Next Step?</h2>
            <a href="/contact" className="cta-button">Get in Touch</a>
        </section>
    );
};

export default CTASection;
